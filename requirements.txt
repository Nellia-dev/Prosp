# requirements.txt

# Para carregar variáveis de ambiente de arquivos .env
python-dotenv~=1.0.0

# SDK oficial do Google para interagir com os modelos Gemini
# Usado tanto pelo Harvester (para análise de imagem, mesmo que falhe com o modelo especificado)
# quanto pelo Analisador de Leads (para processamento de texto).
google-generativeai~=0.6.0

# Para automação de navegador (web scraping na fase Harvester)
playwright~=1.44.0
